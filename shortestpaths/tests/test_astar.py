from shortestpaths import astar

def test_astar():
    graph = None # TODO
    source = None # TODO
    assert astar(graph, source) == None

def test_astar_profiled():
    graph = None # TODO
    source = None # TODO
    assert astar(graph, source, True) == (None, [])
